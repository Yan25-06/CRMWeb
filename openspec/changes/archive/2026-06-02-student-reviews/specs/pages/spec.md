## MODIFIED Requirements

### Requirement: ReviewsPage Layout
The ReviewsPage SHALL replace its placeholder content with a full student review module containing: ReviewSelector (class/student picker), RadarChartPanel (skill radar chart), QuickReviewPanel (tag editor + review form), ReviewHistory (past reviews timeline), and ReportCardModal (export PDF/image).

#### Scenario: Mở tab Nhận Xét
- **WHEN** user vào tab "Nhận Xét"
- **THEN** thấy giao diện chọn lớp + học viên, không thấy placeholder cũ
