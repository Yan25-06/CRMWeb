## MODIFIED Requirements

### Requirement: SchedulePage Layout
The SchedulePage SHALL replace its placeholder content with a full weekly grid schedule view containing: ScheduleHeader (month/year, navigation, add button), WeeklyGrid (7-column grid of schedule cards), and DailyAgenda sidebar.

#### Scenario: Mở tab Lịch Dạy
- **WHEN** user vào tab "Lịch Dạy"
- **THEN** thấy lưới lịch dạy tuần hiện tại, không thấy placeholder cũ
